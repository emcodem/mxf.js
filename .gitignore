# dependencies
node_modules/

# generated debug artifacts (test/e2e/debug.html + yuv-debug.test.ts)
debug-*.png

# large, regenerable test references (ffmpeg yuv422p dumps)
# e.g. frames.yuv: `ffmpeg -i in.mxf -frames:v 12 -pix_fmt yuv422p frames.yuv`
*.yuv

# local Claude Code config / state
.claude/
